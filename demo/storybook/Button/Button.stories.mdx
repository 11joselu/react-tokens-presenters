import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import Button from './Button';
import { Color } from 'react-design-tokens-presenter';
import { scssParser } from 'preprocessors-tokens-parser';

<Meta title="Presenters/Color" component={Color} />

# Button

Buttons are convenient tools when you need more traditional actions

<Preview>
  <Story name="all checkboxes">
    <Button></Button>
  </Story>
</Preview>

# CSS Properties

<!-- Read Button tokens -->

export const scssReq = require.context(
  `!!raw-loader!../`,
  true,
  /.ButtonTokens.scss$/
);

<!-- Create array of files -->

export const files = scssReq.keys();

<div>
  {files
    .filter((filename) => filename.includes('Button'))
    // Read file contents
    .map((filename) => scssReq(filename).default)
    // parse the contents with a tokenizer
    .map(scssParser)
    .flat()
    .map((result) => {
      return (
        <Color variableName={result.declaration} color={result.value}></Color>
      );
    })}
</div>
