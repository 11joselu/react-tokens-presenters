import { Meta, Props } from '@storybook/addon-docs/blocks';
import { readTokensFromFile } from '../utils/tokenReader';
import TokensSummary from './TokensSummary';

<Meta title="TokensSummary" component={TokensSummary} />

# TokensSummary

TokensSummary will paint for each tokens group founded, it will create a table with the representation and value of the token.

<div>
  {<TokensSummary tokens={readTokensFromFile('TokensSummary')}></TokensSummary>}
</div>

## Props

<Props of={TokensSummary} />
