import { Meta, Props } from '@storybook/addon-docs/blocks';
import Border from './Border';
import { readTokensFromFile } from '../../utils/tokenReader';

<Meta title="Presenters/Border" component={Border} />

# Border

<div>
  {readTokensFromFile('Border').map((result, index) => {
    return (
      <div key={index}>
        <Border name={result.declaration} value={result.value}></Border>
        <br></br>
      </div>
    );
  })}
</div>

## Props

<Props of={Border} />
