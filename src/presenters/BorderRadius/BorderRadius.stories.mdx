import { Meta, Props } from '@storybook/addon-docs/blocks';
import { readTokensFromFile } from '../../utils/tokenReader';
import BorderRadius from './BorderRadius';
import Flex from '../../ui/Flex/Flex';
import FlexItem from '../../ui/Flex/FlexItem';

<Meta title="Presenters/BorderRadius" component={BorderRadius} />

# BorderRadius

<Flex>
  {readTokensFromFile('BorderRadius').map((result) => {
    return (
      <FlexItem>
        <BorderRadius
          declaration={result.declaration}
          value={result.value}
        ></BorderRadius>
        <br></br>
      </FlexItem>
    );
  })}
</Flex>

## Props

<Props of={BorderRadius} />
