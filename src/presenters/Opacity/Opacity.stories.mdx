import { Meta, Props } from '@storybook/addon-docs/blocks';
import { readTokensFromFile } from '../../utils/tokenReader';
import Opacity from './Opacity';
import Flex from '../../ui/Flex/Flex';
import FlexItem from '../../ui/Flex/FlexItem';

<Meta title="Presenters/Opacity" component={Opacity} />

# Opacity

<Flex>
  {readTokensFromFile('Opacity').map((result, index) => {
    return (
      <FlexItem key={index}>
        <Opacity name={result.declaration} value={result.value}></Opacity>
      </FlexItem>
    );
  })}
</Flex>

## Props

<Props of={Opacity} />
