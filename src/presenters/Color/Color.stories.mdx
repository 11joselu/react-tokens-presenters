import { Meta, Props } from '@storybook/addon-docs/blocks';
import Color from './Color';
import { readTokensFromFile } from '../../utils/tokenReader.ts';
import Flex from '../../ui/Flex/Flex';
import FlexItem from '../../ui/Flex/FlexItem';

<Meta title="Presenters/Color" component={Color} />

# Color

Convey meaning through color.

## Colors

<Flex>
  {readTokensFromFile('Color').map((result, index) => {
    return (
      <FlexItem key={index}>
        <Color name={result.declaration} value={result.value}></Color>
        <br></br>
      </FlexItem>
    );
  })}
</Flex>

## Props

<Props of={Color} />
