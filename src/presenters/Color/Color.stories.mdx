import { Meta, Props } from '@storybook/addon-docs/blocks';
import Color from './Color';

<Meta title="Presenters/Color" component={Color} />

export const token = `
     /**
       * @tokens Color
       */
        $col-primary: #673ab7;
        $col-secondary: #9c27b0;
    `;

export const expectedResult = [
  {
    declaration: 'col-primary',
    value: '#673ab7',
    token: 'Color',
  },
  {
    declaration: 'col-secondary',
    value: '#9c27b0',
    token: 'Color',
  },
];

# Color

Convey meaning through color.

<div>
  {expectedResult.map((result) => {
    return (
      <>
        <Color name={result.declaration} value={result.value}></Color>
        <br></br>
      </>
    );
  })}
</div>

## Props

<Props of={Color} />
